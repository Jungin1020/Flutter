class User {
  int id;
  String name;
  int gender;
  DateTime signupDate;
  DateTime birthday;
  String phoneNumber;
  String address;

  User(this.id, this.name, this.gender, this.signupDate, this.birthday,
      this.phoneNumber, this.address);
}
